{
  "name": "mitchell-operating-profile",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/prepare-sites.mjs",
    "preview": "astro preview",
    "portfolio:refresh": "node scripts/generate-projects.mjs"
  },
  "dependencies": {
    "astro": "^7.1.6"
  }
}
